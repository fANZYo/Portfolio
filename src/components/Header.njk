<section class="Header">
	<div class="Header__content">
		<h1 class="Header__title">
			{% for keyword in header.title %}
				<span class="Header__keyword">{{ keyword }}</span>
			{% endfor %}
		</h1>

		<p class="Header__blurb">{{ header.blurb | safe }}</p>

		<picture class="Header__picture">
			<source media="(max-width: 719px)" srcset="{{ header.img.small }}">
			<source media="(min-width: 720px)" srcset="{{ header.img.large }}">
			<img class="Header__img" src="{{ header.img.small }}" alt="{{ header.img.alt }}">
		</picture>
	</div>
</section>
